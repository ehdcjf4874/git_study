push : 백업
pull : 복원

git hosting 선택 : github, gitlab

원격저장소와 연결 : 
git remote add origin [원격저장소의 https 주소]
origin은 원격저장소의 별명이다. 원하는 별명을 써도 되지만 관습적으로 origin을 사용한다.

git remote : 원격 저장소를 볼 수 있다.

git remote -v : 원격저장소의 주소를 볼 수 있다.

git push : 로컬저장소에서 원격저장소로 파일을 백업한다.

 

